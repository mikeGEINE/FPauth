(test
 (name unit)
 (package FPauth-strategies)
 (libraries
  alcotest
  dream
  lwt
  lwt.unix
  FPauth-core
  FPauth-strategies
  base
  twostep
 )
 (preprocess
  (pps lwt_ppx)))

; (rule
;  (alias runtest)
;  (action (run %{exe:unit.exe})))